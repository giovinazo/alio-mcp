{
  "_comment": "Add the following block to ~/.claude/settings.local.json (or settings.json) under the top-level 'mcpServers' key.",
  "mcpServers": {
    "alio": {
      "type": "stdio",
      "command": "python3",
      "args": [
        "/absolute/path/to/alio-mcp/alio_mcp.py"
      ],
      "env": {}
    }
  }
}
